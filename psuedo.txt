Global variables:
-localCount
-trains (empty array)

-user types train info in
-on click submit:
    -validate train info
    -add train info to local storage
    -update table with new row for each train
        -calculate next arrival